# Implementation — Reference Documentation

This tree is reference documentation, not a historical record and not a backlog. It is
cited by section from `skills/platforms/evidoxa.md` (e.g. `docs/implementation/06-motion/motion-spec.md`),
which the project's `CLAUDE.md` mandates loading for every UX review. It was written by
the `/implement-design-system` workflow. It changes deliberately, when the implementation
spec itself changes — not incidentally as a side effect of other work — and it is neither
archived nor a set of open tasks.
