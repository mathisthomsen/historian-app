# Design System — Reference Documentation

This tree is reference documentation, not a historical record and not a backlog. It is
cited by section from `skills/platforms/evidoxa.md` (e.g. `docs/design-system/01-ux/research.md §2.4`),
which the project's `CLAUDE.md` mandates loading for every UX review. It was written by
the `/design-system` workflow. It changes deliberately, when the design system itself
changes — not incidentally as a side effect of other work — and it is neither archived
nor a set of open tasks.
